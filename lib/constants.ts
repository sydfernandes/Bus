// Default coordinates for Seville city center (Plaza Nueva)
export const DEFAULT_COORDINATES: [number, number] = [37.3891, -5.9845];

// Default zoom level for the map
export const DEFAULT_ZOOM = 15;

// Default search radius in meters
export const DEFAULT_RADIUS = 1000;

// Maximum number of bus stops to display
export const MAX_BUS_STOPS = 50;
